import Home from "../page";

// /register renders the home page; RegisterModalProvider auto-opens the
// modal on this pathname, making it a shareable registration link.
export default function RegisterPage() {
  return <Home />;
}
